{
  "name": "content-spy-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd content-spy-app && npm install && npm run build",
    "install": "cd content-spy-app && npm install"
  }
}
